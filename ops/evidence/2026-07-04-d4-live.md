# Phase D4 live verification (2026-07-04)

Suite at push: **1020 passed, 0 skipped** (904 → 1020) on fresh pg16 + all 12 migrations.
Commit 9fa41d1; migration 012 applied to prod; `GM_DFS_MONTHLY_BUDGET_CENTS=5000` set on
worker + api.

## Operator console (`/admin/ui`) — live

- `GET /admin/ui` → 200; all six section anchors present; **zero external URLs** (CSP-safe,
  self-contained); every data endpoint 404s without `X-Admin-Token`.
- Gate-1 progress from `/admin/citations/summary` (live): growthmonk.ai baseline **1/3**,
  first_lever_at null → honest "no lever logged yet", treatment None (not 0),
  **59 days to the Sep 1 verdict**.

## Spend rail — live

`/admin/spend?days=30` against prod:
- rollup total **$3.24** — anthropic $2.67 (51 events, audits), dataforseo $0.33 (17),
  openai $0.24 (66 samples), gemini/perplexity $0.00 across 12 error-sample events (honest
  zero-cost rows).
- balance **$0.67** live from `v3/appendix/user_data` (matches the dashboard number checked
  earlier today).
- budget: cap 5000¢, spent 32.8¢ this month, projected 254¢/mo, exceeded=false. The
  pre-spend guard refuses paid DataForSEO calls the moment the calendar-month cap is hit —
  jobs record the refusal note, nothing is silent.

## Refusal log

Migration 012 live; `gm refusal add/list/stats` + `GET/POST /admin/refusals` deployed.
Stats render "DIY share: no refusals logged" until the first entry — the >50% DIY tripwire
now has a place to accumulate data before agency pitching starts.

## Ops hardening

- backup.sh: off-site rclone hook (inert until RCLONE_* env + rclone in image — documented)
  and **self-verifying restore** (RESTORE_VERIFY=1): every weekly dump restores into a
  scratch db on the same server and compares schema_migrations + citation_results counts.
  Next scheduled run (Sun 03:00 UTC) exercises it in prod.
- RLS role-split: `ops/scripts/create_worker_role.sql` live-verified on a scratch pg16
  (28 FORCE-RLS tables, cross-org select 0 rows as gm_worker) + staged-cutover runbook.
  Cutover deliberately NOT executed (solo operator; required before any second operator).
