# Live verification — Phase D2 + D3 against production (2026-07-04)

Operator session, post-takeover. Suite at push time: **904 passed, 0 skipped** on fresh
pg16 + all 11 migrations (baseline at handoff: 685). Registry v1.4.0 / 106 checks.
Commits: c934667 (hygiene) · 40c5667 (backups rail) · 818a878 (D2) · 65b4b0f (D3
contracts) · 3a2da4d (backup pg18) · 79901fb (D3) · 48e8d05 (CI test env fix).

## D2 — competitive intelligence (live, real data)

`GET /admin/sites/4d7b0584…/competitors` on prod, 2026-07 window, minutes after deploy:

- you: growthmonk.ai — 3 tracked queries, rank_top3 0, rank_top10 0, aio_citations 0,
  audit_median 71.5 (n=1)
- ahrefs.com — rank_top3 1, rank_top10 1, audit_median 78.3 (n=1), profile: null, has_data
- partnerstack.com — rank_top3 1, rank_top10 1, audit_median 62.0 (n=1), profile: null
- mywebaudit.com — rank_top3 1, rank_top10 1, audit_median 72.5 (n=1), profile: null

`profile: null` is honest: no `refresh_competitor_profiles` purchase has been made
(DataForSEO trial balance ~$0.83 — deposit is an operator item). The medians come from
the 2026-07-03 comparative audits (`competitor_reference`).

## D3 — e2e probes against prod DB (read/write, idempotence proven)

`ensure_default_schedules(growthmonk.ai)`:
- created: track_serps · keyword_gap · assemble_receipt_monthly ·
  refresh_competitor_profiles · **send_lead_card** (a whatsapp connection row exists
  from the D1 live sim)
- skipped: pull_ads_daily — "no google_ads/meta_ads connection" (honest conditional)
- re-run: created [], existing [all 5] — idempotent. NOTE: send_lead_card will fire
  Mondays 06:00 UTC and FAIL NOISILY until WABA_* env is set on the worker (by design).

`collect_local_pack(growthmonk.ai)`: packs_seen 0, packs_unobserved 0, you_seen 0,
note "no local-pack sighting for tracked queries in the last 35 days" — expected:
existing snapshots predate local-pack entry retention; the next weekly track_serps
tick buys entry-bearing snapshots.

`roas_lines(growthmonk.ai, 2026-07)`: status **awaiting_ad_account**,
booked_consults 1 (the D1 lead), blended None, note "awaiting ad account connection" —
the receipt line renders honestly with zero ad connections. WP-G live e2e stays
deferred until a client links an ad account (blocked-on-client).

## Migrations

010 + 011 applied to prod via `gm db migrate` (manual, per deploy runbook) — clean,
recorded in schema_migrations.

## Backups rail

Railway cron service `backup` (6822ebb3, volume at /data, `0 3 * * 0`). First run
CRASHED on purpose-visible error: pg_dump 16 vs prod server 18 → image bumped to
postgres:18-alpine (3a2da4d). Restore-verification drill still owed
(ops/runbooks/backups.md log table).
