# Product Definition

Status: v1.0 · 2026-07-03 · Derived from `GROWTH-AGENT-SMB-Research-2026-07-03.md` (22-agent eval + fresh 2,001-thread Reddit crawl) fused with the Content Engine outputs spec and platform architecture review.

## 1. The wedge

**Verified AEO Fix Fulfillment with delta receipts.** One engine:

```
audit (103-check, evidence-badged)
  → executed fixes (schema, answer capsules, content refresh — done, not recommended)
    → dated, variance-aware delta re-score vs untouched CONTROL domains
      → client-forwardable proof
```

The market pre-named the purchase trigger (verified verbatim from the fresh crawl): *"it's hard to know which tools are delivering measurable results versus simply reporting data."* Monitoring/scores are dead on arrival ("this is just snake oil," "you're measuring how your own made-up quiz performs against itself"). Only the **action layer with receipts** survives the credibility attack — and the receipt is simultaneously the defense (exec boards demand AI-visibility numbers) and the product.

**Claim discipline (product law, not style):** all deltas are variance-windowed — "named in 7/9 runs, was 1/9" — never binary before/after. Movement is reported **vs control-domain drift**, lever unattributed until Gate-1 evidence supports more. One indefensible attribution claim = 48h correction; a second kills claims-based outbound (kill criterion #8).

## 2. Module verdicts — what passed research, and where it lives in the platform

| Module (research verdict) | Role | Platform mapping |
|---|---|---|
| **AI search / AEO fix loop** — WEDGE | The entire product | `audit` (registry + deterministic scoring + BEV) + `content` (fix-closer) + `delivery` (publish/verify) + `intel` (citation sampling) |
| **Reporting / attribution** — CORE SPINE | Variance-aware delta receipts + WhatsApp booked-lead trend card. Strongest recurring payable pain found anywhere (CallRail: 200k+ SMBs at $45–160/mo for lead attribution). Never standalone, never a dashboard | `delivery.receipts` (content_deltas w/ FK provenance, control panels, binomial CIs) + WhatsApp card via Meta Cloud API (proven on GrowthMonk WABA) |
| **Content writer** — CHECKBOX (fix-closer only) | Jasper −54% revenue/yr; ChatGPT anchors price at $20. Build as the arm that *closes audit findings* (schema, answer capsules, refresh/decay) with re-score receipts. **Never sell it, never name it** | `content-engine/` (wrapped Shakes-peer + blog-buster) invoked only from fix plans; graded by the same registry pre-publish |
| **Reviews / reputation** — CORE, Phase D | Reviews now feed LLM local recommendations (AI-local-search 6%→45% in 12mo). Diagnostic scoring inside the audit ONLY — never review-generation against Birdeye/Podium | New check family in `registry/` (trust-signal checks), Phase D |
| **Ad-spend-leak diagnostic** — CHECKBOX, Phase E, pull-based | Real pain; artifact prices at $0 (free graders everywhere). Manual add-on offered inside open relationships when the Google AI-Max forced-migration wave (Sept 2026–Feb 2027) generates demand. **No build** | No platform module. Manual service if ≥2 clients ask unprompted |
| **SEO optimiser** — CHECKBOX | Rank↔AI-citation overlap collapsed 75%→17-38%. Already exists as checklist steps inside the 103-check registry. No further investment | Existing `registry/` checks. Closed |
| **Landing page / CRO** — SKIP | 6 of 2,001 fresh threads; generation monetizes at ~$1.50/user/mo | Page-quality checks remain as audit line items only |
| **Ad integrations (write)** — SKIP | Platform-absorbed trap (Meta Advantage+, Google AI Max forced migration; Marin is the corpse) | Not built. Ever, absent new evidence |
| **Ad creative / Email / Social** — SKIP | Free floors and platform bundling everywhere; fresh-crawl "demand" partly astroturf | Not built |

### 2.1 Paid-media / ROAS stance (updated 2026-07-03, product decision)

The line the evidence draws is **measurement vs. management** — and the plan now commits to the measurement side:

- **Paid-media measurement (read-only ROAS) — CORE, Phase D, committed.** Daily read-only ingestion of the client's own Google Ads + Meta spend/conversion data, feeding two receipt lines: **blended cost-per-booked-consult** and per-channel spend-vs-booked-lead trend (WhatsApp card + monthly receipt). This is an *extension of the reporting/attribution spine the research ranked #1* — the buyer's largest marketing line item cannot be absent from a receipt product. Concierge integration path requires **zero app review**: client links our Google Ads manager account read-only + adds a Meta Business Manager analyst user (same pattern as the GSC service account). Note: a client's own performance data via their accounts is exact — the "bucketed volumes" problem only affects Keyword Planner estimates without spend, which we don't use.
- **Ad management / write access / bid or budget changes / creative: never built** — platform-absorbed (Meta Advantage+, Google AI Max forced migration Sept 2026–Feb 2027; Marin is the corpse). Unchanged.
- **Ad-spend-leak *audit* as an artifact: Phase E, manual, pull-based only** — the artifact prices at $0 against free graders. Unchanged. But note: once read-only spend data is flowing (Phase D), the manual Phase-E autopsy becomes a half-day query on data we already hold, not a competence fork.
- **Claim discipline still applies:** ROAS lines report the platforms' own conversion figures plus our booked-lead denominator, labeled as such — we never claim attribution precision the data can't support.

### 2.2 Completeness additions (gap sweep, 2026-07-03)

Three modules added after auditing the plan against a "complete growth stack" for the ICP:

| Module | Role | Why it was missing / what it is |
|---|---|---|
| **Paid-media measurement (read-only ROAS)** | CORE, Phase D | See §2.1 |
| **Booked-lead capture** | CORE dependency, Phase D | The attribution spine had a send channel (WhatsApp card) but no ingest: where booked consults come *from*. v1 = WhatsApp Business inbound webhook (click-to-chat attribution) + operator-logged bookings table; booking-platform integrations (Fresha/Zenoti-class) only on client pull. Without this, cost-per-booked-consult has no denominator |
| **Local presence (GBP + listings + NAP + reviews)** | Check family, Phase B–D | Was implicit (entity-consistency checks, Gate-1 directory levers, Phase-D review scoring) — now an explicit registry check family with a fix class, because local presence + reviews are how LLMs pick local recommendations (AI-local-search 6%→45% in 12mo). Diagnostic + fix execution; never review-generation |

**Still deliberately absent, verdicts stand:** email/SMS lifecycle (Klaviyo/GHL land), social scheduling (free floor at $0), ad creative (Meta gives it away), CRM/pipeline (GHL land), speed-to-lead/missed-call textback (GHL-absorbed; revisit only on client pull), landing-page builder (audit line items only), standalone reporting dashboard (do-not-build). A complete *stack* is not the goal — a complete *answer to the buyer's money questions* is: what should we fix, what did it cost, what did it book.

## 3. Target segment

**PRIMARY (revenue spine): GCC multi-location med-spa / dental / aesthetic groups** — Dubai HQ, 3+ locations, one buyer covering N locations. AED 1,850/location autopsy → AED 4,500+/mo group concierge retainer. **Hard cap: 3–5 groups** (solo-operator fulfillment at ≤2h/day). This replaces the single-location Dubai siege.

**SECOND (gated hypothesis, NOT spine): 2–15-person SEO-led agencies** defending $2–4k/mo retainers. Proof Pack $500 = white-label audit + fix instructions + dated variance delta (never executed fixes at that price); execution sold separately at AED 6,500/sprint. Lane opens **only after ≥5 direct paid cards**; verdict date Jan 15, 2027; dies early if >50% of refusals cite free/DIY tools.

**DO NOT SELL TO:** salaried "team-of-one" in-house marketers (no budget authority; "AI employee" framing enrages them) · PPC practitioners (verified DIY anti-buyers) · broke solopreneurs (standing kill) · US multi-location clinics (Mega/KailxLabs foreclose) · $300–500/mo-total-retainer end clients.

**Platform tenancy implication:** org = clinic group or agency; site = domain/GSC property; location pages and per-location prompt panels live under the site. Agencies get orgs whose receipts are white-label-themed — no client-login surface in v1 (share tokens are the client-viewing story).

## 4. Customer outputs by phase

The original 8-output spec survives but is **re-staged for concierge-first** (operator console before self-serve):

| Output | Phase | Change from self-serve spec |
|---|---|---|
| Site/Group Autopsy Report (evidence-badged, per-location) | B | Priced artifact (AED 1,850/location); share-token HTML, print stylesheet — no PDF build |
| Fix Sprint Log (what was executed, when, where) | C | New — concierge receipts need executed-fix provenance, `content_item_findings` claims table |
| **Delta Receipt** (score + GSC clicks + citation rate ± CI vs control) | C | THE artifact. FK provenance to before/after audits + publish events; variance-windowed; control-referenced |
| AI Visibility panel | C | Reframed: never a monitoring dashboard — appears only inside receipts as gap→fix→delta narrative; "beta" label until variance calibration completes |
| WhatsApp booked-lead trend card | D | Weekly card to the group buyer via Meta Cloud API; denominated in booked consults, not citations |
| Proof Pack (white-label) | D (gated) | Agency lane artifact; only if the Oct 15 gate passes |
| Brand Profile Card | C | Onboarding input for fix generation (brandsmith-lite) |
| Opportunity & Refresh Queue | C→E | Operator-facing first (drives the 2h/day fulfillment); customer-facing at productization (Phase E) |

## 5. Pricing anchors & unit economics

Autopsy AED 1,850/location · group concierge AED 4,500+/mo (cap 3–5 groups) · agency Proof Pack $500 · fix sprint AED 6,500. Data COGS ≈ $50–150/mo total (DataForSEO PAYG + free Google/Bing APIs); LLM ≈ $0.80/article, $2.50/audit cap; citation sampling budgeted per plan in `plan_limits` with a "directional" fallback claim when repeats exceed budget (never sample less and overclaim).

## 6. Do-not-build list (standing)

Score/monitoring dashboard or score-led pitch · landing-page/CRO product · ad management or creative generation · standalone reporting SaaS · email/social/all-in-one · "AI agency in a box" positioning · Reddit citation-seeding (radioactive) · per-lead pricing · refund-backed guarantees before Gate-1 evidence · direct-to-SMB ad-audit before Phase E · anything for the salaried team-of-one · OAuth before 2 partners pay manually twice · binary before/after receipts · agency pitching before 5 paid cards · $500 pricing that includes executed fixes · publishing the data study without raw logs.
